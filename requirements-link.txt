# Jetson companion link daemon (Wi-Fi AP/HN API). Install alongside Sirena UI:
#   pip install -r requirements-link.txt
#
# Run:
#   export PYTHONPATH=.
#   python -m nina.link_daemon.main

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
# Dynamixel / action & record bridges (build_app → DynamixelManager)
pyserial>=3.5

# Optional (Jetson): vision + gTTS + YOLO + same stack as Sirena (without PyQt5):
#   pip install -r sirena_ui/requirements-headless.txt
# Object detection needs Ultralytics YOLO (same Python as nina-link):
#   ./.venv-link/bin/python -m pip install ultralytics
# Smaller vision-only shortcut:
#   pip install opencv-python-headless 'numpy>=1.19'
# For YOLO on Jetson you may need the correct PyTorch wheel before ultralytics — see that file's header.
#
# Optional: companion POST /v1/actions/audio/generate (gTTS) on the Jetson:
#   pip install gTTS
#
# ---------------------------------------------------------------------------
# Full companion parity (Map SLAM, Perception, autonomy, vision) needs the
# same Python stack as Sirena UI *without* PyQt5. From repo root on the Jetson:
#   ./scripts/update-nina-link-jetson.sh --sirena-headless --restart
# or:
#   ./.venv-link/bin/pip install -r sirena_ui/requirements-headless.txt
# BreezySLAM compiles a C extension — on aarch64 also:
#   sudo apt install -y build-essential python3-dev
#   # then either pip install the git URL from requirements-headless, or:
#   ./scripts/install-breezyslam-jetson.sh   # read script: targets system python;
#   for nina-link’s venv, prefer pip install -r sirena_ui/requirements-headless.txt
# rplidar / pyrplidarsdk: listed in requirements-headless (Lidar A1 vs Slamtec S2E).
# RealSense (depth): on Jetson aarch64 build librealsense + pyrealsense2 per Intel docs
# (PyPI wheel is often x86-only; see requirements-headless markers).
# ---------------------------------------------------------------------------
