# Local Python venv (alternative to the vision Docker image).
# From repo root:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -U pip && pip install -r requirements.txt
#   export PYTHONPATH=.
#
# Jetson / CUDA: if `pip install ultralytics` fails on torch, install the Jetson
# wheel for your JetPack first, then re-run pip (see NVIDIA / Dusty-NV docs).
# For Roboflow embedded GPU instead of YOLO .pt, see requirements-vision.txt comments.

-r requirements-vision.txt

# carbot_main (motion server, recording CLI)
pyserial>=3.5
rich>=12

# carbot.sh sets VISION_RUNTIME=yolo and VISION_MODEL_PATH=best.pt → Ultralytics (needs torch).
ultralytics>=8.0.0

# Optional web UI: pip install -r requirements-ui.txt
